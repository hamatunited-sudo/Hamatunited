export interface ContentData {
  ar: {
    [key: string]: any;
  };
  en: {
    [key: string]: any;
  };
}

export const useContent = () => {
  // This hook can be expanded later for content management
  return {};
};
